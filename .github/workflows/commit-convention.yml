name: Conventional Commits (main)

# Post-merge tripwire — the counterpart of pr-title-lint.yml, which validates PR
# titles *pre-merge* but cannot see whether the conventional title actually
# reached `main`. This fails loudly when a subject lands on the default branch
# without a valid conventional-commit prefix: a squash-merge setting that used the
# branch commit message instead of the PR title, a direct push, or a squash that
# dropped the prefix — any of which makes semantic-release skip the release.
# It alerts; it cannot gate (the commit is already merged).
#
# Self-managed by this repo (not bootstrap-managed). It mirrors the fleet golden
# tripwire; if the shell grammar below drifts from pr-title-lint.yml, update both.

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  commit-convention:
    name: Validate commit subjects on main
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0 # the pushed range's history must be present to read subjects
      - name: Validate new commit subjects
        env:
          BEFORE: ${{ github.event.before }}
          AFTER: ${{ github.event.after }}
        run: |
          set -euo pipefail
          # Conventional-commit subject grammar — mirrors pr-title-lint.yml. The
          # '!' breaking-change marker is allowed only on a functional type
          # (feat, fix, perf, revert); a non-functional type (docs, chore,
          # refactor, test, style, ci, build) may not carry it.
          pattern='^((feat|fix|perf|revert)(\([^)]+\))?!?|(docs|chore|refactor|test|style|ci|build)(\([^)]+\))?): [^[:space:]].*$'
          # On a branch's first push BEFORE is all-zeros; validate just the tip.
          if printf '%s' "$BEFORE" | grep -qE '^0+$'; then
            revs="$AFTER"
          else
            revs="$(git rev-list --first-parent "${BEFORE}..${AFTER}")"
          fi
          status=0
          for sha in $revs; do
            subject="$(git show -s --format=%s "$sha")"
            if printf '%s\n' "$subject" | grep -qE "$pattern"; then
              echo "ok:   $sha $subject"
            else
              echo "FAIL: $sha $subject"
              status=1
            fi
          done
          if [ "$status" -ne 0 ]; then
            echo
            echo "A commit reached ${GITHUB_REF_NAME:-main} with a non-conventional subject."
            echo "semantic-release only releases conventional commits, so it skips a"
            echo "non-conventional one. Likely cause: a squash merge used the branch commit"
            echo "message instead of the PR title. Set the repo squash-merge default to"
            echo "'PR_TITLE' so PR titles reach main."
            exit 1
          fi
          echo "All new commit subjects are valid conventional commits."
